<UserControl x:Class="OMNIX.Core.Ui.WorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="300" Width="360">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <TextBlock Text="OMNIX" FontSize="18" FontWeight="SemiBold" />
            <Button x:Name="SettingsButton" DockPanel.Dock="Right" Content="Settings" Padding="8,3" Click="OnSettingsRequested" />
        </DockPanel>

        <Border Grid.Row="1" Margin="0,8,0,8" Padding="8" BorderThickness="1" CornerRadius="6" BorderBrush="#40000000">
            <TextBlock x:Name="ContextText" Text="No Office context yet" TextWrapping="Wrap" FontSize="11" />
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="MessagesPanel" />
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PromptBox" MinHeight="56" MaxHeight="140" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
            <Button x:Name="SendButton" Grid.Column="1" Margin="8,0,0,0" Width="64" Content="Send" />
        </Grid>
    </Grid>
</UserControl>
