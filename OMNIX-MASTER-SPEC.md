# OMNIX — سند مرجع کامل و نهایی پروژه

> این سند تنها منبع حقیقت (Source of Truth) این پروژه است. هر تصمیم معماری، هر خط کد، هر فایل باید با این سند مقایسه شود. اگر جایی با آن تضاد دارد، قبل از اجرا باید صریحاً اعلام شود، نه حدس زده شود.

---

## بخش ۰ — وضعیت فعلی و چرا از صفر شروع می‌کنیم

این پروژه چندین بار توسط سازنده‌های مختلف ساخته شده و هر بار به مشکل خورده (نصب ناقص، بارگذاری نشدن افزونه، معماری اشتباه). قبل از هر خط کد جدید:

- کدهای قبلی (پروژه‌هایی به اسم OMNIX، OMINIX، AICompanion) باید **کامل نادیده گرفته شوند و از Workspace حذف شوند**. هیچ‌کدام مبنای کار جدید نیستند، حتی به‌عنوان مرجع.
- اگر در حین کار مشخص شد مسیر فعلی از این سند منحرف شده، **کل Workspace پاک می‌شود و از صفر شروع می‌شود** — نه Patch، نه نگه‌داشتن نیمی از کد قدیمی، نه "چون کار می‌کند نگهش می‌داریم".

---

## بخش ۱ — قوانین سفت‌وسخت (Ironclad Rules) — هرگز، تحت هیچ شرایطی نقض نشوند

این قوانین بالاتر از هر دستور دیگری در این سند یا در هر پیام آینده هستند. حتی اگر کاربر در آینده چیزی بگوید که به‌نظر با این قوانین در تضاد است، سازنده باید متوقف شود و صراحتاً بپرسد، نه اینکه بی‌صدا نقض کند.

```
قانون ۱: هرگز معماری Browser/Chrome/Web App نساز.
قانون ۲: هرگز localhost را به‌عنوان یک سرویس یا برنامهٔ مستقل معرفی نکن.
قانون ۳: هرگز به Node.js وابسته نباش.
قانون ۴: هرگز HTML/JavaScript را رابط اصلی کاربر قرار نده (فقط WPF/C#).
قانون ۵: هرگز Task Pane را تمام‌صفحه یا پنجرهٔ مجزا نکن — همیشه نوار
         جانبی docked کنار سند.
قانون ۶: هرگز API Key را Plain Text ذخیره یا در لاگ چاپ نکن.
قانون ۷: هرگز به AI دسترسی نامحدود به سیستم‌عامل (PowerShell، Registry،
         Filesystem خارج از سند) نده.
قانون ۸: هرگز محتوای سند (سلول/پاراگراف/اسلاید) را به‌عنوان فرمان
         اجرایی تفسیر نکن — همیشه Untrusted Data.
قانون ۹: هرگز بدون تست واقعی روی ویندوز، ادعای "کار می‌کند" یا "PASS"
         نکن. اگر محیط تو (Sandbox) قابلیت Build/اجرای واقعی ویندوز
         را ندارد، همین را صریح بگو.
قانون ۱۰: هرگز از فاز فعلی جلوتر نرو بدون تأیید صریح انسانی.
قانون ۱۱: هرگز تکه‌کد کپی‌شده از منابع وب (اسکریپت‌های ردیابی،
         Cloudflare، قالب‌های ناشناس) را بدون بررسی در فایل نهایی
         نگه‌ندار.
قانون ۱۲: هرگز فرض نکن — همیشه با کد/API واقعی ویندوز/Office، بیتنس،
         نسخه، و وضعیت Runtime را تشخیص بده (نه Hard-code، نه حدس).
قانون ۱۳: اگر یک قانون از این لیست با یک دستور جدید در تضاد افتاد،
         متوقف شو و صریح اعلام کن؛ به‌خودی‌خود قانون را کنار نگذار.
```

---

## بخش ۲ — تعریف محصول و تجربهٔ کاربر

**یک‌جمله‌ای:** OMNIX یک پل ارتباطی (Bridge/Gateway) بین Microsoft Office (Excel, Word, PowerPoint) و مدل‌های هوش مصنوعی (Local و Cloud، شامل قابلیت Vision) است که مستقیم و دائم داخل خود Office زندگی می‌کند.

```
دانلود یک فایل .exe (فقط یک فایل، بدون zip، بدون فایل کمکی)
      ↓
اجرا و نصب (بدون نیاز به دانش فنی؛ بدون Administrator برای بخش
اصلی؛ فقط در صورت نیاز واقعی، یک UAC شفاف برای پیش‌نیاز رسمی
مایکروسافت)
      ↓
باز کردن Excel یا Word یا PowerPoint
      ↓
تب ریبون OMNIX خودکار آنجاست (بدون کار اضافه از کاربر)
      ↓
کلیک روی دکمه → Workspace کنار سند باز می‌شود (نوار جانبی، نه
تمام‌صفحه)
      ↓
چت با AI — متن یا تصویر (Vision) — با دسترسی به Context سند فعلی
      ↓
بستن/باز کردن مجدد برنامه، حتی ری‌استارت ویندوز → همه‌چیز همان‌جا
باقی می‌ماند
```

### تکنولوژی پایه (قطعی):
- **VSTO Add-in** با **C#** و **WPF**.
- سه پروژهٔ میزبان نازک (Excel/Word/PowerPoint) + یک Core مشترک.

---

## بخش ۳ — معماری لایه‌به‌لایه (Layer Architecture)

### لایهٔ ۱ — Office Host Layer (میزبان)
سه پروژهٔ VSTO مجزا (چون VSTO رسماً از چند-میزبانی در یک پروژه پشتیبانی نمی‌کند): `OMNIX.Excel`، `OMNIX.Word`، `OMNIX.PowerPoint`. هرکدام فقط نقطهٔ ورود (`ThisAddIn`) و ثبت Ribbon/TaskPane دارند؛ منطق واقعی در لایهٔ Core مشترک است.

### لایهٔ ۲ — Ribbon & Task Pane Layer
- Ribbon XML مخصوص هر Host (ممکن است جای دکمه در هرکدام کمی متفاوت باشد، ولی رفتار یکسان).
- Task Pane از طریق `CustomTaskPanes.Add()` + `ElementHost` (پل WinForms⇄WPF).

### لایهٔ ۳ — OfficeContextEngine (لایهٔ Context)
هر Host یک Adapter دارد که وضعیت فعلی سند را به یک ساختار داده استاندارد (مثلاً یک کلاس `OfficeContext` مشترک) تبدیل می‌کند:

```
Excel Adapter    → Workbook, Worksheet, Selection, Values, Formulas,
                   Named Ranges, Charts (به‌صورت تصویر برای Vision)
Word Adapter     → Document, Selection, Paragraph, Heading, Table,
                   Track Changes/Comments
PowerPoint Adapter → Presentation, Current Slide (به‌صورت تصویر برای
                   Vision)، Speaker Notes, Shapes/Text
```

### لایهٔ ۴ — Context Limiter
`TokenEstimator` + `Chunker` + `Summarizer` — هرگز یک سند/Workbook کامل بدون فیلتر به AI فرستاده نشود.

### لایهٔ ۵ — AI Gateway
```
UI (چت)
  ↓
AI Gateway (یک نقطهٔ واحد ورود — UI هرگز مستقیم به Provider وصل نشود)
  ↓
Provider Router (اولویت: Local AI در دسترس > Cloud انتخاب‌شده)
  ↓
Provider Adapter (هرکدام ورودی/خروجی یکسان‌سازی‌شده)
```

### لایهٔ ۶ — Provider Adapters
```
Local     → Ollama (11434) / LM Studio (1234) / llama.cpp — Vision
            در صورت پشتیبانی مدل محلی (مثلاً llava، مدل‌های چندوجهی
            Ollama)
Gemini    → پشتیبانی کامل Vision (تصویر + متن) — رایگان روزانه
Groq      → پشتیبانی متن (Vision بسته به مدل موجود در آن لحظه، باید
            هنگام اتصال بررسی و به کاربر گفته شود)
OpenRouter→ چند مدل، برخی Vision‌دار — لیست پویا نمایش داده شود
Custom    → OpenAI-Compatible؛ اگر endpoint از Vision (image_url در
            payload) پشتیبانی کند، فعال شود
```

### لایهٔ ۷ — Tool Executor (Function Calling محدود)
```
ابزارهای فقط‌خواندنی: read_selection, read_document, read_presentation,
                       capture_chart_as_image, capture_slide_as_image
ابزارهای نوشتنی (با تأیید کاربر + Undo):
  write_to_cell, insert_formula, rewrite_selected_text,
  insert_slide, add_speaker_notes, highlight_range
```
هیچ ابزاری خارج از این لیست بدون تصویب صریح این سند اضافه نشود.

### لایهٔ ۷.۵ — Privacy Mode (حالت حریم خصوصی) — الزامی

قبل از هر ارسال داده به هر Provider، کاربر باید یکی از این سه حالت را در Settings انتخاب کرده باشد:

```
Local Only     → داده هرگز از سیستم کاربر خارج نشود؛ اگر Provider
                 انتخابی Cloud بود، درخواست رد شود و پیام واضح
                 نمایش داده شود ("این حالت فقط با AI محلی کار
                 می‌کند")
Cloud Allowed  → داده می‌تواند به Provider Cloud انتخابی ارسال شود
                 (رفتار پیش‌فرض فعلی)
Ask Before Sending → قبل از هر ارسال به Cloud (نه Local)، یک تأیید
                 صریح از کاربر گرفته شود (می‌تواند "برای این
                 جلسه یادآوری نکن" هم داشته باشد)
```

این حالت باید در همان لایهٔ AI Gateway (لایهٔ ۵) اعمال شود — یعنی Gateway قبل از فراخوانی هر Provider Cloud، این تنظیم را چک کند؛ این کنترل نباید فقط در UI باشد و به‌راحتی از طریق کد Provider دور زده شود.

پیش‌فرض هنگام اولین نصب: **Ask Before Sending** (محافظه‌کارانه‌ترین حالت، نه Cloud Allowed).

### لایهٔ ۸ — Storage Layer (محلی)
- تنظیمات/کلید API: DPAPI-encrypted، در `%LOCALAPPDATA%\OMNIX\settings.dat`.
- تاریخچهٔ چت: SQLite یا JSON محلی، با سقف حجمی مشخص (مثلاً حداکثر ۵۰۰ پیام یا ۳۰ روز، قابل تنظیم در Settings؛ Memory هرگز نامحدود رشد نکند).

### لایهٔ ۹ — Diagnostics & Error Layer
کدهای خطای طبقه‌بندی‌شده: `INSTALL_ERROR`, `OFFICE_DETECTION_ERROR`, `REGISTRATION_ERROR`, `UI_ERROR`, `CORE_ERROR`, `NETWORK_ERROR`, `PROVIDER_ERROR`, `AUTH_ERROR`, `MODEL_ERROR`, `TIMEOUT`. هر خطا: کد + پیام قابل‌فهم + جزئیات فنی + پیشنهاد رفع + لاگ. **هرگز پیام عام "اینترنت را بررسی کنید" مگر واقعاً NETWORK_ERROR باشد.**

---

## بخش ۴ — جزئیات دقیق یکپارچگی با هر برنامهٔ Office (چطور وارد Office Package می‌شود)

### ۴.۱ — تشخیص محیط (قبل از هر نصبی)
```
۱. تشخیص بیتنس واقعی Office — نه حدس:
   خواندن HKLM\SOFTWARE\Microsoft\Office\ClickToRun\Configuration
   → مقدار Platform (x86 یا x64)
   اگر ClickToRun نبود (نصب MSI سنتی)، بررسی مسیر نصب واقعی:
   Program Files\Microsoft Office (=64-bit) یا
   Program Files (x86)\Microsoft Office (=32-bit)

۲. تشخیص نسخهٔ Office نصب‌شده (کدام از Excel/Word/PowerPoint
   واقعاً روی سیستم هست) — با خواندن کلیدهای:
   HKCU\Software\Microsoft\Office\16.0\Excel
   HKCU\Software\Microsoft\Office\15.0\Excel
   (و معادل Word/PowerPoint) — نصب فقط برای برنامه‌هایی انجام شود
   که واقعاً روی سیستم هستند.

۳. تشخیص وضعیت VSTO Runtime — هر دو بیتنس جداگانه:
   HKLM\SOFTWARE\Microsoft\VSTO Runtime Setup\v4R      (32-bit view)
   HKLM\SOFTWARE\Wow6432Node\Microsoft\VSTO Runtime Setup\v4R (اگر
   OS 64-bit و بررسی از دید 64 است)
   قانون: اگر Office x64 است، نسخهٔ x64 Runtime باید بررسی و در
   صورت نبود نصب شود — نصب فقط نسخهٔ x86 برای Office x64 کافی
   نیست، حتی اگر AnyCPU باشد.
```

### ۴.۲ — مراحل نصب (به‌ترتیب دقیق)
```
۱. (بدون Administrator) کپی فایل‌ها به %LOCALAPPDATA%\Programs\OMNIX
۲. پاک‌سازی کامل هر نصب قبلی (پوشه + رجیستری + DisabledItems) —
   قبل از نوشتن هر چیز جدید
۳. بررسی و در صورت نیاز نصب VSTO Runtime با بیتنس درست (این تنها
   مرحله‌ای است که ممکن است UAC بخواهد — با توضیح شفاف به کاربر)
۴. وارد کردن گواهی امضای Manifest به CurrentUser\TrustedPublisher
   (بدون نیاز به Administrator)
۵. نوشتن کلیدهای رجیستری Addins برای هر Host نصب‌شده و هر نسخهٔ
   Office موجود (16.0 و/یا 15.0):
   HKCU\Software\Microsoft\Office\{version}\{Host}\Addins\OMNIX
     Description   = "OMNIX AI Office"
     FriendlyName  = "OMNIX"
     LoadBehavior  = 3
     Manifest      = file:///{مسیر نصب}/OMNIX.{Host}.vsto|vstolocal
۶. تأیید نهایی (Verification) — بلافاصله بعد از نوشتن، همان مقادیر
   دوباره خوانده و در install-debug.log ثبت شوند.
۷. ساخت شورتکات Uninstall در Start Menu.
```

### ۴.۳ — رفتار بارگذاری در هر برنامه
```
باز شدن Excel/Word/PowerPoint
      ↓
خواندن کلید Addins مربوط به همان Host
      ↓
بارگذاری Manifest → Assembly Manifest → DLL اصلی از مسیر نصب
      ↓
ساخت نمونهٔ ThisAddIn (سازنده) — همین‌جا اولین خط لاگ
      Startup نوشته شود، قبل از هر کد دیگر
      ↓
Initialize() — آماده‌سازی Application object و CustomTaskPanes
      ↓
CreateRibbonExtensibilityObject() → GetCustomUI() → تب OMNIX در
      ریبون همان برنامه ظاهر می‌شود
```

### ۴.۴ — نکات صادقانه که باید در کد/مستندات ذکر شوند
```
- محل دقیق تب در ریبون به تنظیم insertBeforeMso/insertAfterMso در
  Ribbon XML بستگی دارد؛ اگر تعریف نشود، در انتهای ریبون (بعد از
  Help) قرار می‌گیرد. این باید آگاهانه تصمیم‌گیری شود، نه پیش‌فرض
  بی‌توجه.
- افزونه‌های self-signed بدون اعتماد گواهی می‌توانند بی‌صدا رد شوند
  — همیشه گواهی به TrustedPublisher اضافه شود.
- Excel/Word/PowerPoint افزونه‌های خطادار را در Resiliency\
  DisabledItems ثبت و برای همیشه غیرفعال می‌کنند؛ هر نصب مجدد باید
  این را پاک‌سازی کند.
```

---

## بخش ۵ — مشخصات دقیق Workspace (Task Pane)

```
موقعیت:      نوار جانبی docked، سمت راست (msoCTPDockPositionRight)
عرض پیش‌فرض: 360px — قابل تغییر توسط کاربر (Resizable)، هرگز فضای
             کار در سند را کامل نپوشاند
رفتار:       کاربر باید هم‌زمان بتواند با سند (انتخاب سلول/متن/اسلاید)
             و با پنل کار کند — پنل هرگز مانع تعامل با سند نشود

ساختار داخلی (تب‌ها):
  ├── Chat       (پیش‌فرض، تب اصلی)
  ├── Settings   (Providerها، کلید API، ظاهر، زبان)
  └── About      (اطلاعات سازنده — بخش ۸ همین سند)

طراحی چت:
  - حباب‌های پیام واضح (کاربر/AI با رنگ و راستاچینی متفاوت)
  - Streaming واقعی (نمایش کلمه‌به‌کلمه پاسخ، نه یک‌جا)
  - رندر درست Markdown در پاسخ AI: بلوک‌های کد با Syntax
    Highlighting ساده، جدول‌ها به‌صورت جدول واقعی (نه متن خام)،
    فرمول‌های اکسل در قالب Monospace خوانا — مهم چون این دستیار
    برای کار با داده/فرمول/کد استفاده می‌شود
  - دکمهٔ Stop واقعی: اگر کاربر وسط یک پاسخ Streaming روی Stop بزند
    یا Task Pane/سند را ببندد، درخواست باید واقعاً لغو شود (از طریق
    CancellationToken در فراخوانی HTTP)، نه اینکه در پس‌زمینه ادامه
    پیدا کند یا بعداً خطای بی‌ربط نشان دهد
  - دکمه‌های: New Chat, Copy, Retry, Stop, Clear
  - نوار Context پایین که نشان می‌دهد AI الان چه بخشی از سند را
    "می‌بیند" (مثلاً "Excel: Sheet1 / A1:F50")
  - دکمهٔ پیوست تصویر/اسکرین‌شات برای درخواست‌های Vision (بخش ۶)
  - طراحی مخصوص عرض باریک بهینه شده باشد — نه یک طراحی وب معمولی
    که در نوار باریک فشرده و بد به‌نظر برسد؛ فونت و فاصله‌گذاری با
    توجه به 360px تنظیم شود.
  - تم: Light / Dark / System

رفتار با چند سند/Workbook هم‌زمان باز:
  - هر پنجرهٔ Excel/Word/PowerPoint باز، Task Pane و Context مخصوص
    به خودش دارد (یعنی اگر کاربر دو Workbook مختلف را هم‌زمان باز
    کند، هرکدام چت و Context جدای خودش را دارد، نه یک چت مشترک
    گیج‌کننده بین دو سند متفاوت)
  - تاریخچهٔ چت می‌تواند به‌صورت اختیاری بر اساس نام فایل سند در
    Storage محلی جدا ذخیره شود، تا با باز کردن مجدد همان فایل،
    تاریخچهٔ مرتبط با آن نمایش داده شود
```

---

## بخش ۶ — پشتیبانی از Vision (چندوجهی/تصویری)

Vision یعنی توانایی AI در دیدن و تحلیل محتوای بصری سند — نه فقط متن. این قابلیت باید در معماری از ابتدا لحاظ شود:

```
موارد استفاده:
  - تحلیل یک Chart در Excel ("این نمودار چه روندی نشان می‌دهد؟")
  - تحلیل ظاهر یک اسلاید در PowerPoint ("طراحی این اسلاید چطور است؟")
  - خواندن یک تصویر/اسکرین‌شات که کاربر مستقیم در چت پیست/آپلود
    می‌کند (مثلاً عکس یک نمودار خارجی، یک جدول اسکن‌شده)

پیاده‌سازی فنی:
  ۱. OfficeContextEngine باید بتواند از Chart/Shape/Slide یک تصویر
     (PNG, در حافظه، بدون ذخیرهٔ موقت غیرضروری روی دیسک) بگیرد —
     در Excel از طریق Range.CopyPicture یا Chart.Export موقت؛ در
     PowerPoint از طریق Slide.Export.
  ۲. این تصویر با متن سؤال کاربر، طبق فرمت هر Provider (مثلاً
     base64 + media_type برای Gemini/OpenAI-Compatible)، به AI
     Gateway فرستاده شود.
  ۳. Provider Adapter باید مشخص کند آیا مدل انتخاب‌شده از Vision
     پشتیبانی می‌کند یا نه؛ اگر نه، به کاربر پیام واضح بدهد
     ("این پرووایدر/مدل از تحلیل تصویر پشتیبانی نمی‌کند")، نه
     خطای مبهم.
  ۴. در UI چت، دکمهٔ "پیوست تصویر از سند فعلی" یا "آپلود تصویر"
     وجود داشته باشد.
  ۵. تصویر ارسالی هم مشمول قانون Untrusted Data است (بخش ۱، قانون
     ۸) — اگر تصویر حاوی متنی شبیه فرمان سیستمی بود، صرفاً به‌عنوان
     محتوای تحلیل‌شونده در نظر گرفته شود، نه فرمان.

اولویت Providerهای Vision‌دار (باید در Provider Registry مشخص شود
کدام Vision دارند):
  Gemini      → بله (استاندارد)
  OpenAI-Compatible → بسته به مدل پیکربندی‌شده توسط کاربر
  Local (Ollama) → فقط اگر مدل چندوجهی (مثل llava) نصب باشد
  Groq/OpenRouter → بسته به مدل انتخابی، در لحظهٔ اتصال بررسی شود
```

---

## بخش ۷ — فازبندی کامل + خرد‌فازبندی (Micro-Phasing)

هر فاز کلان به زیرفازهای بسیار کوچک و مستقل تقسیم شده تا سازنده هیچ‌وقت چند کار را هم‌زمان انجام ندهد.

### 🟦 بخش A — پایه و نصب

**فاز ۱ — اسکلت خالی Excel**
```
۱.۱ ساخت پروژهٔ VSTO خالی برای Excel (فقط Hello World Ribbon)
۱.۲ افزودن تب ریبون "OMNIX" با یک دکمهٔ "Open Workspace"
۱.۳ کلیک دکمه → باز شدن Task Pane خالی WPF با متن "OMNIX Workspace"
۱.۴ افزودن لاگ Startup (همان اولین خط سازنده ThisAddIn) —
    %LOCALAPPDATA%\OMNIX\startup-debug.log + AppDomain.UnhandledException
```
خروجی قابل تأیید: Build موفق + (بعداً با نصب واقعی) تب دیده شود.

**فاز ۲ 🔵 Milestone — Installer واقعی**
```
۲.۱ نوشتن installer.iss (Inno Setup) - فقط کپی فایل + رجیستری پایه
۲.۲ افزودن تشخیص بیتنس واقعی Office (بخش ۴.۱)
۲.۳ افزودن بررسی/نصب هر دو بیتنس VSTO Runtime (بخش ۴.۱)
۲.۴ افزودن پاک‌سازی خودکار نصب قبلی + DisabledItems (بخش ۴.۴)
۲.۵ افزودن اعتماد گواهی به TrustedPublisher
۲.۶ افزودن install-debug.log با تمام مراحل بالا
۲.۷ Build واقعی روی GitHub Actions (windows-latest) تا سبز شود
۲.۸ Release یک فایل exe واحد
۲.۹ تست با Windows Defender فعال (نه خاموش‌شده برای تست): نصب و
    اجرای فایل exe روی یک سیستم/Runner با Defender در حالت پیش‌فرض
    روشن؛ بررسی که آیا Defender فایل را Quarantine می‌کند یا
    به‌عنوان تهدید (مثلاً به‌خاطر الگوی «نصب‌کنندهٔ بدون امضا +
    ثبت رجیستری + نصب گواهی در Trust Store») علامت‌گذاری می‌کند.
    اگر Defender هشدار داد، این باید در گزارش صادقانه ذکر شود
    (نه پنهان یا نادیده گرفته شود) و در صورت امکان راهکار کاهش
    آن (مثلاً کاهش رفتارهای مشکوک، یا در آینده Code Signing طبق
    بخش ۱۰.۲) بررسی شود.
```
خروجی قابل تأیید: نصب/حذف تمیز (خودکار در CI) + تب واقعاً در Excel کاربر دیده شود (تأیید انسانی با اسکرین‌شات) + هر دو فایل لاگ درست نوشته شوند + نتیجهٔ تست Defender (سالم یا هشدار) مستند شده باشد.

### 🟦 بخش B — چت و AI Gateway پایه

**فاز ۳ — OfficeContextEngine (فقط Excel، فقط متن)**
```
۳.۱ خواندن Selection فعلی (آدرس + مقادیر)
۳.۲ خواندن نام Sheet فعلی
۳.۳ نمایش این اطلاعات به‌صورت متن ساده در پایین Task Pane
```

**فاز ۴ — UI چت (بدون AI، فقط Echo)**
```
۴.۱ طراحی حباب پیام (بدون منطق واقعی) طبق بخش ۵
۴.۲ Textbox ورودی + دکمهٔ Send
۴.۳ رفتار Echo: پیام کاربر عیناً به‌عنوان جواب "AI" نمایش داده شود
۴.۴ تست: پیام ارسال شود، UI فریز نکند
```

**فاز ۵ 🔵 Milestone — اتصال واقعی Gemini (فقط متن)**
```
۵.۱ ساخت ChatService با متد ساده SendMessage(text) → Gemini API
۵.۲ افزودن فیلد API Key موقت (هنوز بدون DPAPI - فقط برای تست)
۵.۳ اتصال دکمهٔ Send به ChatService واقعی
۵.۴ Build واقعی + تست واقعی: سؤال بپرس، پاسخ واقعی از Gemini بیاید
```

**فاز ۶ — لایهٔ AI Gateway (Refactor)**
```
۶.۱ ایجاد اینترفیس IProviderAdapter
۶.۲ انتقال منطق Gemini به یک GeminiAdapter مجزا پشت این اینترفیس
۶.۳ تست: رفتار فاز ۵ دقیقاً بدون تغییر باقی بماند
```

### 🟦 بخش C — Providerها و تنظیمات

**فاز ۷ — Provider Registry + Groq/OpenRouter**
```
۷.۱ ساخت کلاس ProviderRegistry با لیست ثابت اولیه
۷.۲ افزودن GroqAdapter
۷.۳ افزودن OpenRouterAdapter
۷.۴ UI انتخاب Provider در تب Settings (فقط Dropdown، بدون کلید هنوز)
```

**فاز ۸ — Custom Provider**
```
۸.۱ فرم افزودن Provider سفارشی (Name, Base URL, API Key, Model)
۸.۲ دکمهٔ Test Connection با نتیجهٔ واقعی (موفق/خطا)
```

**فاز ۹ — تشخیص Local AI**
```
۹.۱ Probe به Ollama (11434) و LM Studio (1234) در بک‌گراند
۹.۲ نمایش وضعیت Available/Not Available در Settings
۹.۳ اولویت‌بندی: اگر Local در دسترس بود، پیش‌فرض روی آن باشد
```

**فاز ۱۰ 🔵 Milestone — DPAPI + Settings کامل**
```
۱۰.۱ رمزنگاری کلید API با DPAPI قبل از ذخیره در دیسک
۱۰.۲ UI کامل Settings (همهٔ Providerها + کلید + تست اتصال)
۱۰.۳ Build واقعی + تست: ذخیره/بازیابی کلید، بررسی که در فایل روی
      دیسک Plain Text نیست
```

### 🟦 بخش D — About و امنیت پایه

**فاز ۱۱ — تب About**
```
۱۱.۱ محتوای متنی (بخش ۸ همین سند)
۱۱.۲ آیکون‌های رسمی کلیک‌پذیر (Simple Icons، جاسازی‌شده در XAML)
۱۱.۳ همان اطلاعات در README پروژه
```

**فاز ۱۲ — Error Architecture**
```
۱۲.۱ تعریف Enum کدهای خطا (بخش ۳، لایهٔ ۹)
۱۲.۲ Try/Catch در هر لایه با کد خطای مشخص، نه پیام عام
۱۲.۳ Retry با Backoff برای خطای شبکهٔ موقت
۱۲.۴ Failover: اگر Provider فعلی مدام خطا داد، پیشنهاد Provider بعدی
```

### 🟦 بخش E — قابلیت‌های پیشرفته

**فاز ۱۳ — Context Limiter**
```
۱۳.۱ TokenEstimator ساده (تخمین تعداد کاراکتر/توکن)
۱۳.۲ Chunker برای داده‌های بزرگ (تقسیم به بخش‌های قابل مدیریت)
۱۳.۳ جلوگیری از ارسال کل Workbook بدون فیلتر
```

**فاز ۱۴ — ابزارهای فقط‌خواندنی + Vision پایه**
```
۱۴.۱ Function Calling: read_selection, read_document
۱۴.۲ capture_chart_as_image (Excel) - خروجی PNG در حافظه
۱۴.۳ ارسال تصویر به Gemini (Vision) + نمایش پاسخ
۱۴.۴ دکمهٔ "پیوست تصویر از سند" در UI چت
```

**فاز ۱۵ 🔵 Milestone — ابزارهای نوشتنی + Undo**
```
۱۵.۱ write_to_cell با پیش‌نمایش پیش از اعمال
۱۵.۲ rewrite_selected_text (Word) با پیش‌نمایش
۱۵.۳ اتصال به Undo Stack بومی Office
۱۵.۴ Build واقعی + تست واقعی: AI یک تغییر پیشنهاد دهد، کاربر تأیید
      کند، تغییر اعمال و با Ctrl+Z قابل بازگشت باشد
```

**فاز ۱۶ — محافظت Prompt Injection**
```
۱۶.۱ تست با سندی که حاوی متن "Ignore previous instructions..." است
۱۶.۲ اطمینان از اینکه AI این را داده می‌بیند، نه فرمان
```

### 🟦 بخش F — Word و PowerPoint

**فاز ۱۷ — Word**
```
۱۷.۱ پروژهٔ VSTO Word (نازک) + Ribbon مشابه
۱۷.۲ OfficeContextEngine.WordAdapter
۱۷.۳ اتصال به همان Core مشترک (چت/Provider/Settings یکسان)
```

**فاز ۱۸ — PowerPoint**
```
۱۸.۱ پروژهٔ VSTO PowerPoint (نازک) + Ribbon مشابه
۱۸.۲ OfficeContextEngine.PowerPointAdapter + capture_slide_as_image
۱۸.۳ اتصال به همان Core مشترک
```

**فاز ۱۹ 🔵 Milestone — Installer واحد سه‌میزبانه**
```
۱۹.۱ به‌روزرسانی installer.iss برای ثبت هر سه Host (فقط برای
      برنامه‌هایی که واقعاً روی سیستم نصب‌اند)
۱۹.۲ Build واقعی + تست واقعی هر سه برنامه روی سیستم کاربر
```

### 🟦 بخش G — تست نهایی

**فاز ۲۰ — Persistence**
```
۲۰.۱ تست: نصب → باز/بسته کردن هر برنامه چند بار → تب هنوز هست
۲۰.۲ تست: ری‌استارت ویندوز → تب هنوز هست
```

**فاز ۲۱ — Uninstall تمیز**
```
۲۱.۱ تست: بعد از Uninstall، هیچ فایل/کلید رجیستری باقی نماند
```

**فاز ۲۲ 🔵 Milestone — Release نهایی**
```
۲۲.۱ (در صورت وجود گواهی واقعی) امضای Authenticode
۲۲.۲ Release نهایی نسخهٔ ۱.۰.۰ عمومی
```

---

## بخش ۸ — About / Creator (محتوای دقیق، ثابت، غیرقابل تغییر)

```
عنوان: Powered by Mr Ali

توضیح (دقیقاً همین متن، بدون تغییر یک کلمه):
"Created and developed by Mr Ali, an independent developer building
practical digital tools, automation solutions, and useful projects.
Follow the channels above for updates, new projects, and useful content."

لینک‌ها (دقیقاً همین‌ها، هرگز کوتاه یا تغییر داده نشوند):
Email             → mailto:Ali.hekmati2026@gmail.com
Telegram          → https://t.me/Mr_Ali_2025
Telegram Channel  → https://t.me/Ali_shortcuts
Facebook          → https://www.facebook.com/AliShortcuts
TikTok            → https://www.tiktok.com/@ali_shortcuts
Instagram         → https://www.instagram.com/ali_shortcuts
YouTube           → https://www.youtube.com/@Ali_Shortcuts
```

الزامات UI:
```
- هر آیکون، آیکون رسمی برند (از Simple Icons یا معادل رسمی و
  آزاد)، جاسازی‌شده به‌صورت وکتور در خود برنامه (بدون وابستگی به
  اینترنت برای نمایش آیکون)
- کل ردیف (آیکون + نام + نام‌کاربری) یک عنصر کلیک‌پذیر واحد باشد
- کلیک → Process.Start با UseShellExecute=true برای باز کردن لینک
- طراحی تمیز، مینیمال، هم‌راستا با بقیهٔ UI (کارت‌های گرد با hover)
- همین اطلاعات، همین آیکون‌ها، همین لینک‌ها در README پروژهٔ
  GitHub هم قرار گیرند — بخش کوچک و مرتب، نه تبلیغاتی
```

---

## بخش ۹ — فرمت اجباری گزارش بعد از هر زیرفاز/فاز

```
────────────────────────────
PHASE: [شماره فاز و زیرفاز، مثلاً ۲.۳]
STATUS: [کد نوشته شد / (فقط Milestone) Build واقعی سبز شد - لینک]
FILES CHANGED: [لیست دقیق]
WHAT IT DOES: [توضیح دقیق]
COMPLIANCE CHECK:
  [ ] هیچ کدام از ۱۳ قانون سفت‌وسخت بخش ۱ نقض نشده
  [ ] هیچ فایل/قابلیت خارج از دامنهٔ این زیرفاز اضافه نشده
  [ ] (Milestone) Build واقعی سبز - لینک Run
  [ ] Workspace هنوز docked است، نه تمام‌صفحه
  [ ] (اگر مربوط است) Privacy Mode قبل از هر ارسال Cloud رعایت شده
  [ ] (فقط فاز ۲ و ۱۹ - Milestone نصب) تست با Windows Defender روشن
      انجام و نتیجه مستند شده
KNOWN ISSUES: [هرچیز ناقص یا نامطمئن]
────────────────────────────
```
هیچ زیرفاز/فاز بعدی بدون تأیید صریح «برو فاز بعد» شروع نشود.

---

## بخش ۱۰ — پیشنهادهای ارتقا (فازهای اختیاری، بعد از تکمیل بخش ۷)

این‌ها ویژگی‌هایی هستند که در نسخهٔ اول اسپک نبودند اما برای یک محصول واقعاً کامل و بلندمدت لازم می‌شوند. اولویتشان پایین‌تر از بخش ۷ است — فقط بعد از تکمیل کامل فاز ۲۲ به این‌ها پرداخته شود:

**۱۰.۱ — مکانیزم Update**
```
- بررسی دوره‌ای نسخهٔ جدید (مثلاً چک یک فایل version.json در
  GitHub Releases، نه یک سرور اختصاصی)
- Update هرگز نباید تنظیمات/کلید API/تاریخچهٔ چت کاربر را پاک کند
  (Migration بین نسخه‌ها)
- به کاربر اطلاع داده شود نسخهٔ جدید هست، نه نصب خودکار بی‌اجازه
```

**۱۰.۲ — امضای دیجیتال واقعی (Code Signing)**
```
- جایگزینی گواهی موقت با یک گواهی Authenticode واقعی (خریداری‌شده
  یا از طریق برنامه‌های رایگان مثل SignPath برای پروژه‌های متن‌باز)
- هدف: حذف کامل هشدار SmartScreen "Windows protected your PC"
- تا قبل از آن، این باید در README صادقانه ذکر شود (که همین الان
  هم هست) - این تغییر نمی‌کند، فقط اولویت بالاتری برای رفعش
  گذاشته می‌شود
```

**۱۰.۳ — گزارش خطا/Telemetry اختیاری (Opt-in)**
```
- فقط با رضایت صریح کاربر (Toggle در Settings، پیش‌فرض خاموش)
- فقط کدهای خطا و اطلاعات فنی غیرشخصی (نسخه، بیتنس، نوع خطا) —
  هرگز محتوای سند یا چت
- هدف: کمک به رفع باگ در نسخه‌های بعدی بدون نقض حریم خصوصی
```

**۱۰.۴ — نصب چندکاربره روی یک سیستم**
```
- روشن کردن این‌که نصب per-user است (هر کاربر ویندوز باید جدا نصب
  کند) و این محدودیت را در README مستند کردن
- بررسی سناریوی Centralized Deployment مایکروسافت ۳۶۵ برای
  سازمان‌ها به‌عنوان یک مسیر آیندهٔ مجزا (نه بخشی از این نسخه)
```

**۱۰.۵ — دسترسی‌پذیری (Accessibility)**
```
- ناوبری کامل با کیبورد در Task Pane (Tab، Enter، Esc)
- سازگاری با Screen Reader (برچسب‌های AutomationProperties.Name در
  WPF برای هر دکمه/کنترل)
```

**۱۰.۶ — کنترل هزینه/سهمیهٔ Cloud Provider**
```
- نمایش هشدار ساده وقتی کاربر به نزدیکی سهمیهٔ رایگان روزانه
  (Gemini/Groq) می‌رسد، در صورتی که API آن Provider این اطلاع را
  در Response Header یا مشابه بدهد
- این یک Best-effort است، نه تضمین دقیق مالی
```

**۱۰.۷ — بومی‌سازی (Localization)**
```
- زبان پیش‌فرض رابط: انگلیسی
- معماری آماده برای افزودن فارسی (RTL) در آینده - رشته‌های متنی از
  همان ابتدا در یک منبع مرکزی (Resource Dictionary) نگه داشته
  شوند، نه Hard-code داخل XAML، تا افزودن زبان بعداً ساده باشد
```

---

## بخش ۱۱ — دستور شروع به سازندهٔ جدید

```text
این سند را کامل بخوان، از اول تا آخر. اگر چیزی نامشخص است، همین
الان بپرس - قبل از نوشتن حتی یک خط کد.

سپس فقط زیرفاز ۱.۱ (بخش ۷) را بساز. هیچ زیرفاز یا فاز دیگری را
شروع نکن. بعد از اتمام، گزارش را دقیقاً با فرمت بخش ۹ بده و منتظر
تأیید بمان.

قوانین بخش ۱ همیشه، در تمام فازها، بدون استثنا، رعایت شوند.
بخش ۱۰ (پیشنهادهای ارتقا) تنها بعد از تکمیل کامل بخش ۷ در نظر
گرفته شود، نه زودتر.
```

---

## بخش ۱۲ — الزام نهایی و قطعی تحویل — بدون استثنا

```text
هدف نهایی و تنها معیار پذیرش این پروژه، صرفاً یک چیز است:

              یک فایل واحد با پسوند .exe

که وقتی روی ویندوز اجرا شود، خودش را کامل نصب کند و بلافاصله
داخل Excel/Word/PowerPoint در دسترس باشد.

هیچ‌کدام از موارد زیر معیار پذیرش نیستند و به‌تنهایی کافی نیستند:

  ✗ "کد نوشته شد"
  ✗ "Build موفق بود" (بدون Release واقعی)
  ✗ "تست‌های خودکار CI سبز شدند" (بدون تأیید انسانی روی ویندوز واقعی)
  ✗ توضیح معماری، گزارش تحلیلی، یا هر مستند دیگر بدون فایل واقعی

در پایان هر فاز Milestone (علامت 🔵) و در پایان کل پروژه، سازنده
موظف است:

  ۱. یک فایل exe واحد و واقعی، از طریق GitHub Release یا Artifact
     قابل‌دانلود، تحویل دهد.
  ۲. لینک مستقیم دانلود همان فایل را ارائه دهد.
  ۳. تأیید کند که این فایل به‌تنهایی (بدون zip، بدون فایل کمکی
     دیگر) قابل اجرا و نصب است.

اگر سازنده تا این مرحله نرسیده، پروژه یا آن فاز تمام‌شده تلقی
نمی‌شود - صرف‌نظر از هر میزان کد یا مستندی که تولید شده باشد.
```
